name: CI
on:
  push:
  pull_request:

jobs:
  build:
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Trivial test
        run: |
          echo "Running trivial test"
          test 1 -eq 1
